---
name: 交易分析
description: 执行完整的交易分析流程，包括市场检查、数据同步、蜂群咨询和信号执行
category: 交易
tags: [analysis, trading, workflow]
---

# 交易分析命令

执行完整的交易分析工作流程，为Commander提供结构化的交易信号和决策支持。

## 命令语法
```bash
/trade-analysis [选项]
```

## 实现方式
此命令使用稳定的Python脚本 `scripts/trade_analysis.py` 来执行，避免了bash执行大段代码的不稳定性问题。

## 参数选项

### 基本参数
- `--sectors <板块列表>` - 要分析的板块，用逗号分隔 (默认: ALL)
- `--min-confidence <数值>` - 最低信号置信度 (默认: 0.75)
- `--max-orders <数量>` - 每次运行最大执行订单数 (默认: 2)

### 执行控制
- `--skip-sync` - 跳过数据同步 (默认: false)
- `--dry-run` - 仅分析不执行交易 (默认: false)
- `--verbose` - 显示详细执行过程 (默认: false)

### 输出选项
- `--format <格式>` - 输出格式: table|json (默认: table)

### 帮助信息
- `--help` - 显示此帮助信息

## 支持的参数
脚本支持以下完整的参数列表：
```bash
python scripts/trade_analysis.py [--sectors SECTORS] [--min-confidence MIN_CONFIDENCE] [--max-orders MAX_ORDERS] [--skip-sync] [--dry-run] [--verbose] [--format json|table] [--help]
```

## 使用示例

### 基础分析
```bash
# 执行默认分析
/trade-analysis

# 试运行模式
/trade-analysis --dry-run
```

### 定制分析
```bash
# 分析特定板块，提高置信度要求
/trade-analysis --sectors TECH,FINANCE --min-confidence 0.80 --max-orders 3

# 快速分析跳过数据同步
/trade-analysis --skip-sync --max-orders 1

# 详细模式分析
/trade-analysis --verbose --min-confidence 0.70
```

### 输出格式控制
```bash
# JSON格式输出
/trade-analysis --format json

# 组合参数
/trade-analysis --sectors TECH --dry-run --verbose --format json
```

## 执行流程

### 1. 市场状态检查
- 检查当前交易时段
- 评估市场开放状态
- 验证基础交易条件

### 2. 数据质量评估
- 检查监控标的数据新鲜度
- 识别过期数据
- 评估数据完整性

### 3. 市场背景分析
- SPY趋势分析
- 市场波动率评估
- 宏观环境判断

### 4. 蜂群智能咨询
- 并发执行多个策略实例
- 收集高置信度交易信号
- 信号聚合和过滤

### 5. 风险评估和决策
- 仓位大小计算 (Kelly Criterion)
- 投资组合集中度检查
- 市场环境风险评估

### 6. 订单执行准备
- 通过安全层验证所有订单
- 准备执行参数
- 生成执行报告

## 输出格式

命令返回结构化的分析结果，包含：

### 市场状态
```json
{
  "market_session": "REGULAR",
  "market_open": true,
  "data_quality": "GOOD",
  "total_symbols": 50,
  "fresh_symbols": 48
}
```

### 分析结果
```json
{
  "high_confidence_signals": [
    {
      "signal": "SHORT_PUT_SPREAD",
      "target": "NVDA",
      "confidence": 0.82,
      "reasoning": "IV rank 85%，中性情绪，技术支撑确认",
      "max_risk": 400,
      "capital_required": 500
    }
  ],
  "execution_summary": {
    "signals_analyzed": 15,
    "high_confidence_count": 3,
    "execution_time": 2.3
  }
}
```

## 集成说明

### 🔒 架构锁定：禁止嵌入式脚本实现

**重要原则**：此命令必须使用预定义的技能函数，**绝不允许**使用嵌入式大脚本实现。

#### 禁止的错误实现方式
```bash
# 🚫 绝对禁止：嵌入式大脚本
python_script = f'''
import sys
# 100+ 行复杂逻辑...
'''
bash(f'python3 -c "{python_script}"')
```

#### 强制要求的正确实现
```python
# ✅ 唯一正确：使用技能函数（已包含架构验证）
from skills.workflow_skills import run_market_health_check, run_full_trading_analysis

health = run_market_health_check()
if health.get('market_open') and health.get('data_quality') != 'CRITICAL':
    analysis = run_full_trading_analysis(
        sectors=["TECH"],
        min_confidence=0.75,
        max_orders_per_run=2
    )
    return analysis
else:
    return {"status": "skipped", "reason": "market_or_data_issues"}
```

### 核心技能函数（必须使用）
此命令**只能**调用以下预定义技能：
- `run_market_health_check()` - 市场健康检查
- `run_full_trading_analysis()` - 完整交易分析（包含数据同步和蜂群咨询）
- `run_position_risk_analysis()` - 风险分析
- `place_order_with_guard()` - 安全订单执行

### 架构验证检查
执行前必须确认：
- ☐ 没有使用 `python3 -c` 或嵌入式大脚本
- ☐ 每个步骤都调用预定义技能函数
- ☐ 没有直接调用底层MCP工具
- ☐ 代码行数不超过30行

## 错误处理

常见错误及解决方案：

### 数据质量问题
- **错误**: `数据质量 CRITICAL，跳过蜂群咨询`
- **解决**: 检查市场数据连接，等待数据刷新

### 市场关闭
- **错误**: `市场已关闭，跳过交易执行`
- **解决**: 使用 `--skip-sync` 进行离线分析

### 参数验证失败
- **错误**: `置信度参数超出有效范围 (0.0-1.0)`
- **解决**: 使用 `--help` 查看参数范围

## 性能指标

- **响应时间**: 通常 < 3秒
- **数据同步**: 30秒内完成（如需要）
- **信号生成**: 1-2秒
- **风险评估**: < 1秒

---
*此命令通过Slash Command Integration提案实现，将复杂的多步骤分析流程简化为单一命令调用。*